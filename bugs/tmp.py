if True:
    if True:
        if True:
            if True:
                if True:
                    if True:
                        from some_library.some_package.some_module import (
                            some_function,  # noqa: PLC0415
                        )
